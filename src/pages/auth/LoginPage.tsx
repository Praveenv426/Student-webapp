import React from 'react';
import { LoginForm } from '@/components/auth/LoginForm'; // student login form uses student context

const StudentLoginPage: React.FC = () => {
  return <LoginForm />;
};

export default StudentLoginPage;
